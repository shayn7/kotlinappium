package mobile_platforms

import enums.MobilePlatform

class IosPlatform(mobilePlatform: MobilePlatform) : AbstractPlatform(mobilePlatform) {
    override fun initCapabilities() {

    }

    override fun installedAppCapabilities() {
    }
}